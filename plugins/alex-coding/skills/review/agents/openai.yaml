interface:
  display_name: "Review"
  short_description: "Review one PR through merge or handoff"
  default_prompt: "Use $review to start or continue the independent reviewer for this named PR through merge or a user-attention handoff."
